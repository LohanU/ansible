- name: loops demo
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      msg: "{{ item }}" 
    loop:
    - mysql
    - nginx
    - postfix
    - httpd
    